<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" 
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <uses-feature
        android:name="android.hardware.sensor.light"
        android:required="false" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ConexionAndroidSpring"
        android:usesCleartextTraffic="true">

        <!-- Google Maps API Key -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyCLkjmYlVQ4s-fEQXVbjTKDN3TiS93vmfE" />

        <!-- Registro: rota según el sensor -->
        <activity
            android:name=".RegisterActivity"
            android:exported="false"
            android:label="Registro"
            android:screenOrientation="fullSensor" />

        <!-- Favoritos: rota -->
        <activity
            android:name=".FavoritosActivity"
            android:exported="false"
            android:screenOrientation="fullSensor" />

        <!-- Detalle Anime: rota -->
        <activity
            android:name=".AnimeDetailActivity"
            android:screenOrientation="fullSensor" />

        <!-- Emisión: rota -->
        <activity
            android:name=".EmisionActivity"
            android:screenOrientation="fullSensor" />

        <!-- Perfil: rota -->
        <activity
            android:name=".ProfileActivity"
            android:exported="false"
            android:screenOrientation="fullSensor" />

        <!-- Mis Animes: rota -->
        <activity
            android:name=".MisAnimesActivity"
            android:exported="false"
            android:screenOrientation="fullSensor" />

        <!-- Crear anime: rota -->
        <activity
            android:name=".CreateAnimeActivity"
            android:screenOrientation="fullSensor" />

        <!-- Main: rota -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="fullSensor" />

        <!-- Login: rota -->
        <activity
            android:name=".Login"
            android:exported="true"
            android:label="Login"
            android:screenOrientation="fullSensor">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
